# Hit it — ProGuard/R8 rules.
# Release minification is disabled for the MVP (see build.gradle.kts). When enabling it later,
# Room, Hilt, and Compose ship their own consumer rules; add app-specific keeps here as needed.
