<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Uses only built-in Android Material theme — no external library needed -->
    <style name="Theme.SpotifyMashupGenerator" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/spotify_green</item>
        <item name="android:statusBarColor">@color/background</item>
    </style>
</resources>
